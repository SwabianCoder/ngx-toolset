name: PR validation @ngx-toolset/loading-spinner

on:
  pull_request:
    branches:
      - main
      
jobs:
  path-filter:
    name: Path filter
    runs-on: ubuntu-latest
    outputs:
      changes: ${{ steps.changes.outputs.project }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check for changes in path
        uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            project:
              - 'projects/loading-spinner/**'
  call-pr-validation-workflow:
    name: Call PR validation workflow
    needs: path-filter
    if: ${{ needs.path-filter.outputs.changes == 'True' }}
    uses: ./.github/workflows/pr_validation.yml
    with:
      PACKAGE_NAME: loading-spinner
    secrets: inherit
